import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-issue-27085',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
